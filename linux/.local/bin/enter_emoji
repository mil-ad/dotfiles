#!/usr/bin/env bash

assert_installed type-clipboard-content rofimenu wl-copy

chosen=$(rofimenu < ~/.local/share/emojis.txt | cut -d " " -f1)

[ "$chosen" != "" ] || exit

echo -n "$chosen" | wl-copy && type-clipboard-content
