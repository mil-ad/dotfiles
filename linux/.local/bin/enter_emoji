#!/usr/bin/env bash

assert_installed paste-clipboard-content rofimenu wl-copy

chosen=$(rofimenu < ~/.local/share/emojis.txt | cut -d " " -f1)

[ "$chosen" != "" ] || exit

echo -n "$chosen" | wl-copy && paste-clipboard-content
