#!/usr/bin/env bash

assert_installed gum mods

source "$HOME/.tokens"

get_prompt() {
    gum input --placeholder="Prompt ..." --prompt="💭 " --no-show-help --cursor.foreground="#d79921"
}

first_prompt=$(get_prompt) && printf "💭 %s\n🤖\n" "$first_prompt" && mods <<< "$first_prompt" || exit

while true; do
    next_prompt=$(get_prompt) && printf "💭 %s\n🤖\n" "$next_prompt" && mods --continue-last <<< "$next_prompt" || break
done
