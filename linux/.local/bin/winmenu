#!/usr/bin/env bash

assert_installed swaymsg wl-pick

IFS=$'\t' read -r type id toplevel app title < <(wl-pick) &&
case $type in
    window) swaymsg "[con_id=$id] focus" ;;
    output) swaymsg "focus output $id" ;;
esac
