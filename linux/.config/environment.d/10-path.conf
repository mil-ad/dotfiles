# Environment for systemd --user units (and anything they spawn).
# Read by systemd-environment-d-generator(8) when the user manager starts
# or on `systemctl --user daemon-reload`.

PATH=${HOME}/.local/bin:${HOME}/.cargo/bin:${PATH}
