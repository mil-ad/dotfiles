(defcfg
  process-unmapped-keys yes
)

(defsrc
  caps
  h j k l
  n m
)

(deflayer base
  @cap
  h j k l
  n m
)

(deflayer extend
  _
  left down up rght
  mlft mrgt
)

(defalias
  cap (tap-hold 200 200 esc (layer-while-held extend))
)
