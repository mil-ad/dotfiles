#!/usr/bin/env bash

browsers=(helium-browser chromium chromium-browser)

for b in "${browsers[@]}"; do
    if command -v "$b" &>/dev/null; then
        exec "$b" --app="https://chatgpt.com/"
    fi
done

echo "Error: none of ${browsers[*]} found in PATH" >&2
exit 1
