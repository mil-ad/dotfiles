#!/usr/bin/env bash

assert_installed wl-copy rg
set -eo pipefail

echo $1 | rg -o "\d+\.\d+(v\d+)?.pdf$" | xargs -r -I {} ~/.scripts/roam_arxiv_authors "https://arxiv.org/abs/{}" | wl-copy && notify-send "📋 Copied to clipboard" || notify-send "❌ Failed to copy"
